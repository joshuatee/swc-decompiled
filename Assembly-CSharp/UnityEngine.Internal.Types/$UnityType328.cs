using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType328 : $UnityType
	{
		public unsafe $UnityType328()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 426940) = ldftn($Invoke200);
			*(data + 426968) = ldftn($Invoke201);
			*(data + 426996) = ldftn($Invoke202);
			*(data + 427024) = ldftn($Invoke203);
			*(data + 427052) = ldftn($Invoke204);
		}
	}
}
