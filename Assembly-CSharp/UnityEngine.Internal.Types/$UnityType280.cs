using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType280 : $UnityType
	{
		public unsafe $UnityType280()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 420444) = ldftn($Invoke68);
			*(data + 420472) = ldftn($Invoke69);
			*(data + 420500) = ldftn($Invoke70);
			*(data + 420528) = ldftn($Invoke71);
		}
	}
}
