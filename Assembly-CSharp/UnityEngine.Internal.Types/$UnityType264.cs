using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType264 : $UnityType
	{
		public unsafe $UnityType264()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 418652) = ldftn($Invoke4);
			*(data + 418680) = ldftn($Invoke5);
			*(data + 418708) = ldftn($Invoke6);
			*(data + 418736) = ldftn($Invoke7);
		}
	}
}
