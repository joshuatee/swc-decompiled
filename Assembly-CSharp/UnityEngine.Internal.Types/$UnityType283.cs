using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType283 : $UnityType
	{
		public unsafe $UnityType283()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 420780) = ldftn($Invoke80);
			*(data + 420808) = ldftn($Invoke81);
			*(data + 420836) = ldftn($Invoke82);
			*(data + 420864) = ldftn($Invoke83);
		}
	}
}
