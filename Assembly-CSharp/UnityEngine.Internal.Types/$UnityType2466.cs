using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType2466 : $UnityType
	{
		public unsafe $UnityType2466()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 966948) = ldftn($Invoke0);
			*(data + 966976) = ldftn($Invoke1);
			*(data + 967004) = ldftn($Invoke2);
			*(data + 967032) = ldftn($Invoke3);
			*(data + 967060) = ldftn($Invoke4);
			*(data + 967088) = ldftn($Invoke5);
		}
	}
}
