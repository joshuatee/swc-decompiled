using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType281 : $UnityType
	{
		public unsafe $UnityType281()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 420556) = ldftn($Invoke72);
			*(data + 420584) = ldftn($Invoke73);
			*(data + 420612) = ldftn($Invoke74);
			*(data + 420640) = ldftn($Invoke75);
		}
	}
}
