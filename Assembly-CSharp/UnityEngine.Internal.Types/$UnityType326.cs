using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType326 : $UnityType
	{
		public unsafe $UnityType326()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 426660) = ldftn($Invoke190);
			*(data + 426688) = ldftn($Invoke191);
			*(data + 426716) = ldftn($Invoke192);
			*(data + 426744) = ldftn($Invoke193);
			*(data + 426772) = ldftn($Invoke194);
		}
	}
}
