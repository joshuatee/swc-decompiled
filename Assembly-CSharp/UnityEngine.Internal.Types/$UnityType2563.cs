using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType2563 : $UnityType
	{
		public unsafe $UnityType2563()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 996040) = ldftn($Invoke0);
			*(data + 996068) = ldftn($Invoke1);
			*(data + 996096) = ldftn($Invoke2);
			*(data + 996124) = ldftn($Invoke3);
		}
	}
}
