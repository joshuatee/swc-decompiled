using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType266 : $UnityType
	{
		public unsafe $UnityType266()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 418876) = ldftn($Invoke12);
			*(data + 418904) = ldftn($Invoke13);
			*(data + 418932) = ldftn($Invoke14);
			*(data + 418960) = ldftn($Invoke15);
		}
	}
}
