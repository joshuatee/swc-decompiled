using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType324 : $UnityType
	{
		public unsafe $UnityType324()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 426380) = ldftn($Invoke180);
			*(data + 426408) = ldftn($Invoke181);
			*(data + 426436) = ldftn($Invoke182);
			*(data + 426464) = ldftn($Invoke183);
			*(data + 426492) = ldftn($Invoke184);
		}
	}
}
