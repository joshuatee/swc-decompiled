using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType284 : $UnityType
	{
		public unsafe $UnityType284()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 420892) = ldftn($Invoke84);
			*(data + 420920) = ldftn($Invoke85);
			*(data + 420948) = ldftn($Invoke86);
			*(data + 420976) = ldftn($Invoke87);
		}
	}
}
