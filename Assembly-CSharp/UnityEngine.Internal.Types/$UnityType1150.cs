using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType1150 : $UnityType
	{
		public unsafe $UnityType1150()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 644360) = ldftn($Invoke114);
			*(data + 644388) = ldftn($Invoke115);
			*(data + 644416) = ldftn($Invoke116);
		}
	}
}
