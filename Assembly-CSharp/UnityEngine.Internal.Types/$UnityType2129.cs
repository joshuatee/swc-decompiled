using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType2129 : $UnityType
	{
		public unsafe $UnityType2129()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 858756) = ldftn($Invoke0);
			*(data + 858784) = ldftn($Invoke1);
			*(data + 858812) = ldftn($Invoke2);
			*(data + 858840) = ldftn($Invoke3);
			*(data + 858868) = ldftn($Invoke4);
			*(data + 858896) = ldftn($Invoke5);
		}
	}
}
