using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType22 : $UnityType
	{
		public unsafe $UnityType22()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 350556) = ldftn($Invoke10);
			*(data + 350584) = ldftn($Invoke11);
			*(data + 350612) = ldftn($Invoke12);
			*(data + 350640) = ldftn($Invoke13);
			*(data + 350668) = ldftn($Invoke14);
		}
	}
}
