using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType325 : $UnityType
	{
		public unsafe $UnityType325()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 426520) = ldftn($Invoke185);
			*(data + 426548) = ldftn($Invoke186);
			*(data + 426576) = ldftn($Invoke187);
			*(data + 426604) = ldftn($Invoke188);
			*(data + 426632) = ldftn($Invoke189);
		}
	}
}
