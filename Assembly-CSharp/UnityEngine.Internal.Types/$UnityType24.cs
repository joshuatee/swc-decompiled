using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType24 : $UnityType
	{
		public unsafe $UnityType24()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 350836) = ldftn($Invoke20);
			*(data + 350864) = ldftn($Invoke21);
			*(data + 350892) = ldftn($Invoke22);
			*(data + 350920) = ldftn($Invoke23);
			*(data + 350948) = ldftn($Invoke24);
		}
	}
}
