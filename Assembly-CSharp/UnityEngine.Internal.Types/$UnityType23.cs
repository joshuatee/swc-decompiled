using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType23 : $UnityType
	{
		public unsafe $UnityType23()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 350696) = ldftn($Invoke15);
			*(data + 350724) = ldftn($Invoke16);
			*(data + 350752) = ldftn($Invoke17);
			*(data + 350780) = ldftn($Invoke18);
			*(data + 350808) = ldftn($Invoke19);
		}
	}
}
