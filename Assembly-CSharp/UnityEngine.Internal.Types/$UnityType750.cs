using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType750 : $UnityType
	{
		public unsafe $UnityType750()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 548152) = ldftn($Invoke0);
			*(data + 548180) = ldftn($Invoke1);
			*(data + 548208) = ldftn($Invoke2);
			*(data + 548236) = ldftn($Invoke3);
			*(data + 548264) = ldftn($Invoke4);
		}
	}
}
