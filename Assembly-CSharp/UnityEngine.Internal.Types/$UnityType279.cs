using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType279 : $UnityType
	{
		public unsafe $UnityType279()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 420332) = ldftn($Invoke64);
			*(data + 420360) = ldftn($Invoke65);
			*(data + 420388) = ldftn($Invoke66);
			*(data + 420416) = ldftn($Invoke67);
		}
	}
}
