using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType2440 : $UnityType
	{
		public unsafe $UnityType2440()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 957540) = ldftn($Invoke0);
			*(data + 957568) = ldftn($Invoke1);
			*(data + 957596) = ldftn($Invoke2);
			*(data + 957624) = ldftn($Invoke3);
			*(data + 957652) = ldftn($Invoke4);
			*(data + 957680) = ldftn($Invoke5);
		}
	}
}
