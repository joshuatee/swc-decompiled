using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType276 : $UnityType
	{
		public unsafe $UnityType276()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 419996) = ldftn($Invoke52);
			*(data + 420024) = ldftn($Invoke53);
			*(data + 420052) = ldftn($Invoke54);
			*(data + 420080) = ldftn($Invoke55);
		}
	}
}
