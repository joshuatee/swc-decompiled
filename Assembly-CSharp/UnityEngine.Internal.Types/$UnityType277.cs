using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType277 : $UnityType
	{
		public unsafe $UnityType277()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 420108) = ldftn($Invoke56);
			*(data + 420136) = ldftn($Invoke57);
			*(data + 420164) = ldftn($Invoke58);
			*(data + 420192) = ldftn($Invoke59);
		}
	}
}
