using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType20 : $UnityType
	{
		public unsafe $UnityType20()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 350276) = ldftn($Invoke0);
			*(data + 350304) = ldftn($Invoke1);
			*(data + 350332) = ldftn($Invoke2);
			*(data + 350360) = ldftn($Invoke3);
			*(data + 350388) = ldftn($Invoke4);
		}
	}
}
