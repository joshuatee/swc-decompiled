using System;

namespace UnityEngine.Internal.Types
{
	public sealed class $UnityType2125 : $UnityType
	{
		public unsafe $UnityType2125()
		{
			IntPtr data = UnityEngine.Internal.$Metadata.data;
			*(data + 857440) = ldftn($Invoke0);
			*(data + 857468) = ldftn($Invoke1);
			*(data + 857496) = ldftn($Invoke2);
			*(data + 857524) = ldftn($Invoke3);
			*(data + 857552) = ldftn($Invoke4);
			*(data + 857580) = ldftn($Invoke5);
		}
	}
}
